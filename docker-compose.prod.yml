services:
  gestfrontend:
    image: ghcr.io/hafizbkr/gestfrontend:latest
    container_name: gestfrontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
